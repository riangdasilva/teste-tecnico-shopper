{
  "scripts": {
    "dev": "tsc && node ./dist/server.js",
    "start": "rm -rf ./dist/* && rm -rf ./public/* && tsc && cd ../client && npm run build && mv ./dist/* ../server/public && cd ../server && node ./dist/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "prisma": "^5.2.0",
    "typescript": "^5.2.2"
  }
}